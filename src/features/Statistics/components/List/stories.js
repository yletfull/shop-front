import React from 'react';
import List from './index';

export default {
  title: 'Features/Statistics/components/List',
  component: List,
};

const Template = (args) => <List {...args} />;

export const Playground = Template.bind({});
Playground.args = {
};
