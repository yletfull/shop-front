import React from 'react';
import ListOfSites from './index';

export default {
  title: 'Features/Statistics/components/ListOfSites',
  component: ListOfSites,
};

const Template = (args) => <ListOfSites {...args} />;

export const Playground = Template.bind({});
Playground.args = {
};
