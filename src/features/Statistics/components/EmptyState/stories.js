import React from 'react';
import EmptyState from './index';

export default {
  title: 'Features/Statistics/components/EmptyState',
  component: EmptyState,
};

const Template = (args) => <EmptyState {...args} />;

export const Playground = Template.bind({});
Playground.args = {
};
